apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "service-payment.fullname" . }}-test-connection"
  labels:
    {{- include "service-payment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "service-payment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
