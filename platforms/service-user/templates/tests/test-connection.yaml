apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "service-user.fullname" . }}-test-connection"
  labels:
    {{- include "service-user.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "service-user.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
